<%= form_for @member, remote: true do |f| %>

    <%= f.text_field :name, placeholder: "Name" %>
    <%= f.number_field :team, value: 0, class: "cant-see-me"  %>
    <%= f.check_box :leader, placeholder: false, class: "cant-see-me"  %>

    <%= f.button :submit %>

<% end %>
